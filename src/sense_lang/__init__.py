"""Sense — a programming language for probabilistic and autonomous computation.

This package currently implements the Phase 1 deterministic core described in
docs/ROADMAP.md: lexer, parser, AST, a light type checker, and a tree-walking
interpreter. AI-native primitives (model, ask, tool, memory, agent, ...) land
in later phases.
"""

__version__ = "0.20.0"
